Follow KISS, DRY, SOLID principles, clean naming conventions, functional programing, optimize for programmer happiness, convention over configuration, a "menu is omakase" philosophy, meaning the solution provides curated, integrated system of tools that work best together, rather than requiring the developer to assemble every component. Inline comments are code smell, decompose them and create smaller functions with documentation comment instead. Magic strings are code smell use constant uppercase variables.  